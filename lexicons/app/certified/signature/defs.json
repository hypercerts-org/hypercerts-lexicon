{
  "lexicon": 1,
  "id": "app.certified.signature.defs",
  "description": "Common type definitions for cryptographic signatures attached to records.",
  "defs": {
    "list": {
      "type": "array",
      "description": "Reusable array of cryptographic signatures attesting to a record's content. Uses an open union to support inline signatures and strong references to remote attestation proof records.",
      "items": {
        "type": "union",
        "refs": ["app.certified.signature.inline", "com.atproto.repo.strongRef"]
      },
      "maxLength": 100
    }
  }
}
