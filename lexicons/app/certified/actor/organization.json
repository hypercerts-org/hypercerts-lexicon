{
  "lexicon": 1,
  "id": "app.certified.actor.organization",
  "defs": {
    "main": {
      "type": "record",
      "description": "Extended metadata for an organization actor. Complements the base actor profile with organization-specific fields like legal structure and reference links.",
      "key": "literal:self",
      "record": {
        "type": "object",
        "required": ["createdAt"],
        "properties": {
          "organizationType": {
            "type": "array",
            "description": "Legal or operational structures of the organization (e.g. 'nonprofit', 'ngo', 'government', 'social-enterprise', 'cooperative').",
            "items": {
              "type": "string",
              "maxLength": 1000,
              "maxGraphemes": 100
            },
            "maxLength": 10
          },
          "urls": {
            "type": "array",
            "description": "Additional reference URLs (social media profiles, contact pages, donation links, etc.).",
            "items": {
              "type": "ref",
              "ref": "#urlItem"
            },
            "maxLength": 20
          },
          "location": {
            "type": "ref",
            "ref": "com.atproto.repo.strongRef",
            "description": "A strong reference to the location where the organization is based. The record referenced must conform with the lexicon app.certified.location."
          },
          "foundedDate": {
            "type": "string",
            "format": "datetime",
            "description": "When the organization was established."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Client-declared timestamp when this record was originally created."
          }
        }
      }
    },
    "urlItem": {
      "type": "object",
      "description": "A labeled URL reference.",
      "required": ["url"],
      "properties": {
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The URL.",
          "maxLength": 10000,
          "maxGraphemes": 2048
        },
        "label": {
          "type": "string",
          "description": "Optional human-readable label for this URL (e.g. 'Twitter', 'Donation page').",
          "maxLength": 640,
          "maxGraphemes": 64
        }
      }
    }
  }
}
